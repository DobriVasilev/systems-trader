# Trading Systems Engine - Python Dependencies

# Core
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0

# Technical Analysis
# NOTE: Requires C library first!
# Mac: brew install ta-lib
# Linux: apt-get install ta-lib
# Windows: Download from https://www.ta-lib.org/
TA-Lib>=0.4.28

# Hyperliquid
hyperliquid-python-sdk>=0.1.0
eth-account>=0.11.0

# Database (sqlite3 is built-in, but we need aiosqlite for async)
aiosqlite>=0.19.0

# Web API (for dashboard)
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0

# Async
aiohttp>=3.9.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Dev tools
black>=23.0.0
mypy>=1.0.0
