# Trading API Configuration

# Port to run on (default: 4000)
TRADING_API_PORT=4000

# API key for authentication between Vercel app and this server
# Generate with: openssl rand -base64 32
TRADING_API_KEY=your-secure-api-key-here

# Server-side encryption key for wallet private keys
# Should match the one used in the Vercel app
WALLET_ENCRYPTION_KEY=your-encryption-key-here

# Allowed origins (comma-separated)
ALLOWED_ORIGINS=https://dobri.org,http://localhost:3000
